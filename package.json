{
  "name": "dance-studio-chat",
  "version": "1.0.0",
  "description": "Dance Studio Class Booking System",
  "scripts": {
    "build": "gulp build",
    "build:dev": "gulp build:dev",
    "start": "gulp serve",
    "dev": "npm run build:dev && netlify dev",
    "functions:install": "cd netlify/functions && npm install"
  },
  "dependencies": {
    "nodemailer": "^6.9.7",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "gulp": "^4.0.2",
    "gulp-rename": "^2.0.0",
    "gulp-replace": "^1.1.4",
    "@supabase/supabase-js": "^2.38.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
